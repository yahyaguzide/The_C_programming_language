#include <stdio.h> 

int power(int m, int n); 

int for return 0; 
i; 
(i = 0; i < 10; ++i) printf("%d %d %d\n", i, power(2,i), power(-3,i)); 

int power(int base, int n) { 
} 
int p = for i, p; 
1; (i = 1; i <= n; ++i) p = p * base; 
return p; 

